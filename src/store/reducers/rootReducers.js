
const initState = {
    users: [
        { id: 1, name: 'Don' },
        { id: 2, name: 'Dinh' }
    ]
}
const rootReducers = (state = initState, action) => {
    return state;
}
export default rootReducers;